'use client';
// DeveloperEngagePath — payment/fee conditions removed.
// Developers proceed directly to workspace without path selection.
export function DeveloperEngagePath({ leadId, currentPath }: { leadId: string; currentPath?: string | null }) {
  return null;
}
